<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.github.ffewi</groupId>
    <artifactId>arz-common</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <dependencies>
        <!-- 将本地 jar 声明为 system scope 依赖 -->
        <dependency>
            <groupId>com.github.ffewi</groupId>
            <artifactId>arz-common-internal</artifactId>
            <version>1.0.0</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/libs/arz-common-1.0.jar</systemPath>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- 把 system scope 的 jar 也一起打包进来 -->
                            <includeSystemScope>true</includeSystemScope>
                            <!-- 去掉多余的 shade 后缀，直接替换原始 jar -->
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>